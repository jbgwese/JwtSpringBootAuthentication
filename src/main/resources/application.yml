management:
  endpoints:
    web:
      exposure:
        include: '*'


spring:
  datasource:
    url: jdbc:postgresql://db:5432/assignment
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate :
        format_sql: true
#      database-platform: org.hibernate.dialect.PostgreSQLDialect
#      database: postgres

